---
- name: create topic
  command: >
    echo "DEFINE TOPIC({{ host.mqvars.active_qmgr.topics.topic01.name }})
    TOPICSTR('{{ host.mqvars.active_qmgr.topics.topic01.topicstr }}')
    REPLACE"
    | {{ common.mq.directories.onhosts.mq_binaries }}runmqsc {{ host.mqvars.active_qmgr.name }}